# Legacy model artifacts

These files were generated during earlier baseline/ensemble experiments and are retained for traceability. They are not required by the maintained Streamlit app, which loads the exported Tab-MFM artifact from `final_model/`.
